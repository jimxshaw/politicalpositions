<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="190dp"
    android:background="@drawable/img_nav_drawer_header">

    <!-- Guidelines -->>
    <!-- Subtitle : 56dp -->
    <!-- Name: Roboto Medium, 14sp, #FFFFFF -->
    <!-- Email address: Roboto Regular, 14sp, #FFFFFF -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:paddingBottom="15dp"
        android:paddingLeft="15dp">

        <TextView
            android:id="@+id/nav_drawer_header_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="sans-serif-medium"
            android:gravity="bottom"
            android:text="@string/app_name"
            android:textColor="#000000"
            android:shadowDx="0.0"
            android:shadowDy="0.0"
            android:shadowRadius="7.5"
            android:shadowColor="#FFFFFF"
            android:textSize="14sp"/>

        <TextView
            android:id="@+id/nav_drawer_header_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="sans-serif"
            android:gravity="bottom"
            android:text="@string/navigation_drawer_header_subtitle"
            android:textColor="#000000"
            android:shadowDx="0.0"
            android:shadowDy="0.0"
            android:shadowRadius="7.5"
            android:shadowColor="#FFFFFF"
            android:textSize="14sp"/>


    </LinearLayout>

</RelativeLayout>